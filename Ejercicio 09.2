import impares  
  
  
def main():  
numeros = input("ingrese la lista de numeros separados por espacios: ")  
print(impares.impares(numeros))  
  
  
if __name__ == '__main__':  
main()

## impares ##

from functools import reduce  
 
def impares(numeros):  
return reduce(lambda x,y:x+y ,filter(lambda n : n%2 == 1, map(int,numeros.split())))
